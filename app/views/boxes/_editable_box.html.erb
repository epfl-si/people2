<%= render "shared/item_visibility", item: box, extra_class: []%>
<%= link_to icon_text('trash-2', 'action.delete'), box_path(box), class: "btn btn-secondary btn-sm", method: :delete, data: {turbo_stream: true, turbo_method:'delete', turbo_confirm: t('action.confirm')} if box.user_destroyable? %>
<%= tag.h2(box.t_title) %>