<p>
<% skip_edit ||= false %>
<%= link_to icon_text('trash-2', t('action.delete')), polymorphic_path(item), class: "btn btn-secondary btn-sm", method: :delete, data: {turbo_stream: true, turbo_method:'delete'} %>
<%= link_to icon_text('edit', t('action.edit')), edit_polymorphic_path(item), class: "btn btn-secondary btn-sm", data: {turbo_stream: true} unless skip_edit %>
</p>
