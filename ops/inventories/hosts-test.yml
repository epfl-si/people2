# Test deployment on openshift test cluster.
all:
  hosts:
    next:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
  vars:
    inventory_environment: "dev"

    APP_HOSTNAME: "people-test.epfl.ch"
    APP_REPLICAS: 1
    JOB_REPLICAS: 1
    APP_VISIBILITY: "private"
    ENABLE_ADOPTION: "yes"
    # ISA_URL: https://isa.epfl.ch/services
    OC_CLUSTER: "https://api.ocpitst0001.xaas.epfl.ch:6443"
    OC_NAMESPACE: svc0033t-people
    RUNENV: prod
    RAILS_ENV: development
    RELEASE: test
    USE_LOCAL_ELEMENTS: "yes"
    LEGACY_RW_URL: "https://test-people.epfl.ch"
    EPFLAPI_BACKEND_URL: "https://api.epfl.ch/v1"
    WSGETPEOPLE_CACHE: 0
    NAME_CHANGE_REQUEST_EMAIL: "giovanni.cangiani+peonc@epfl.ch"
    # EPFLAPI_BACKEND_URL: "https://api-test.epfl.ch/v1"


    REDIS_DISK_SIZE: 1
